(function(){
  function LoginCtrl($uibModal, $uibModalInstance, $cookies) {
    this.ok = fucntion(username) {
      $uibModalInstance.close(this.login)
    }
  }

  angular
    .module('blocChat')
    .controller('LoginCtrl', ['$uibModal', '$uibModalInstance', LoginCtrl])
})
